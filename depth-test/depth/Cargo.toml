[package]
name = "depth"
version = "0.1.1"
edition = "2021"
publish = false

[dependencies]
onnx-embedding = { path = "../../onnx-embedding" }
ort = { version = "2.0.0-rc.9", features = [ "cuda", "ndarray", "load-dynamic" ]}
tempfile = "3.19.1"
zip = "4.0.0"
